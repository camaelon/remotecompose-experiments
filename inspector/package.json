{
  "name": "remote-compose-inspector",
  "version": "1.0.0",
  "description": "RemoteCompose Inspector UI Development & Build Pipeline",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
